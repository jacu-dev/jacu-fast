{
  "schema_version": 1,
  "checks": [
    {
      "id": "core-unit",
      "kind": "cargo-test",
      "program": "cargo",
      "args": ["test", "--locked", "--lib"],
      "cwd": ".",
      "timeout_seconds": 600,
      "minimum_tests": 1,
      "reuse": "never",
      "inputs": []
    }
  ],
  "delivery": {"required_check_ids": ["core-unit"]}
}
